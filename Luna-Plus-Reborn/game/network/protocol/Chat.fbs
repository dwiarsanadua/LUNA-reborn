include "Types.fbs";
include "PacketType.fbs";

namespace luna.protocol;

table ChatMessage {
    sender_id: uint32;
    sender_name: string;
    message: string (required);
    channel: ChatChannel;
    timestamp: uint64;
}
